import { Paper, Table, TableBody, TableCell, TableContainer, TableHead, TableRow, Typography } from "@mui/material";
import type { SystemStat } from "./types";
import { formatIst } from "./utils";
export const ApiStatsSystemActivity = ({ rows }: { rows: SystemStat[] }) => <Paper variant="outlined" sx={{ borderRadius: 2, overflow: "hidden" }}><Typography variant="h6" fontWeight={700} sx={{ p: 2 }}>System activity</Typography><TableContainer><Table size="small"><TableHead><TableRow>{["System / source","Operation","Total events","Successes","Warnings","Failures","Hour (IST)"].map(x=><TableCell key={x}>{x}</TableCell>)}</TableRow></TableHead><TableBody>{rows.map((r,i)=><TableRow key={`${r.bucket_start}-${r.system}-${r.operation}-${i}`}><TableCell>{r.system}</TableCell><TableCell>{r.operation}</TableCell><TableCell>{r.total_events}</TableCell><TableCell>{r.successes}</TableCell><TableCell>{r.warnings}</TableCell><TableCell>{r.failures}</TableCell><TableCell sx={{whiteSpace:"nowrap"}}>{formatIst(r.bucket_start)}</TableCell></TableRow>)}</TableBody></Table></TableContainer></Paper>;
